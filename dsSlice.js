// slice will copies the value of array into another array
// Without changing the main array

const arr = [3, 4, 5, 6, 7, 8, 9, 10];
const newArr = arr.slice(3, 6); // newArr will have elements before index 6
console.log(newArr);
